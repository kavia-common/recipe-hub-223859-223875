# Base URL of the FastAPI backend (must be reachable from the browser).
# Example: https://my-backend.example.com
NEXT_PUBLIC_API_BASE_URL=

# Optional: the deployed site URL (useful for auth redirect flows if backend supports it).
NEXT_PUBLIC_SITE_URL=
